export enum OrderStatus {
  PENDING = 'pending',
  PARTIALLY_ACCEPTED = 'partially_accepted',
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
  IN_PREPARATION = 'in_preparation',
  SHIPPED = 'shipped',
  DELIVERED = 'delivered',
  CANCELLED = 'cancelled',
}
